name: anti-trigger
description: Should not trigger on unrelated prompts
prompt: "How do I deploy my Azure Function to production?"
expected:
  skill_activated: false
graders:
  - type: text
    name: no-optimization-advice
    config:
      not_contains:
        - "token budget"
        - "reduce tokens"
