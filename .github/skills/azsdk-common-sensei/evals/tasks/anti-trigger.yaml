name: anti-trigger
description: Should not trigger on unrelated prompts
prompt: "How do I configure my Azure Storage account for geo-redundancy?"
expected:
  skill_activated: false
graders:
  - type: text
    name: no-sensei-advice
    config:
      not_contains:
        - "frontmatter"
        - "Ralph loop"
