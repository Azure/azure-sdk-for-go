package fabric

// Copyright (c) Microsoft and contributors.  All rights reserved.
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
// http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
//
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by Microsoft (R) AutoRest Code Generator 2.2.21.0
// Changes may cause incorrect behavior and will be lost if the code is regenerated.

import (
	"github.com/Azure/go-autorest/autorest"
	"github.com/Azure/go-autorest/autorest/azure"
	"github.com/Azure/go-autorest/autorest/validation"
	"net/http"
)

// InfraRoleInstancesClient is the fabric Admin Client
type InfraRoleInstancesClient struct {
	ManagementClient
}

// NewInfraRoleInstancesClient creates an instance of the InfraRoleInstancesClient client.
func NewInfraRoleInstancesClient(subscriptionID string) InfraRoleInstancesClient {
	return NewInfraRoleInstancesClientWithBaseURI(DefaultBaseURI, subscriptionID)
}

// NewInfraRoleInstancesClientWithBaseURI creates an instance of the InfraRoleInstancesClient client.
func NewInfraRoleInstancesClientWithBaseURI(baseURI string, subscriptionID string) InfraRoleInstancesClient {
	return InfraRoleInstancesClient{NewWithBaseURI(baseURI, subscriptionID)}
}

// Get get an infra role instance.
//
// location is location of the resource. infraRoleInstance is name of an infra role instance.
func (client InfraRoleInstancesClient) Get(location string, infraRoleInstance string) (result InfraRoleInstance, err error) {
	if err := validation.Validate([]validation.Validation{}); err != nil {
		return result, validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "Get")
	}

	req, err := client.GetPreparer(location, infraRoleInstance)
	if err != nil {
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Get", nil, "Failure preparing request")
		return
	}

	resp, err := client.GetSender(req)
	if err != nil {
		result.Response = autorest.Response{Response: resp}
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Get", resp, "Failure sending request")
		return
	}

	result, err = client.GetResponder(resp)
	if err != nil {
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Get", resp, "Failure responding to request")
	}

	return
}

// GetPreparer prepares the Get request.
func (client InfraRoleInstancesClient) GetPreparer(location string, infraRoleInstance string) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"infraRoleInstance": autorest.Encode("path", infraRoleInstance),
		"location":          autorest.Encode("path", location),
		"subscriptionId":    autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}

	preparer := autorest.CreatePreparer(
		autorest.AsGet(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances/{infraRoleInstance}", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{})
}

// GetSender sends the Get request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) GetSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client, req)
}

// GetResponder handles the response to the Get request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) GetResponder(resp *http.Response) (result InfraRoleInstance, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}

// List get a list of all infra role instances at a location.
//
// location is location of the resource. filter is oData filter parameter.
func (client InfraRoleInstancesClient) List(location string, filter string) (result InfraRoleInstanceList, err error) {
	if err := validation.Validate([]validation.Validation{}); err != nil {
		return result, validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "List")
	}

	req, err := client.ListPreparer(location, filter)
	if err != nil {
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", nil, "Failure preparing request")
		return
	}

	resp, err := client.ListSender(req)
	if err != nil {
		result.Response = autorest.Response{Response: resp}
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", resp, "Failure sending request")
		return
	}

	result, err = client.ListResponder(resp)
	if err != nil {
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", resp, "Failure responding to request")
	}

	return
}

// ListPreparer prepares the List request.
func (client InfraRoleInstancesClient) ListPreparer(location string, filter string) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"location":       autorest.Encode("path", location),
		"subscriptionId": autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}
	if len(filter) > 0 {
		queryParameters["$filter"] = autorest.Encode("query", filter)
	}

	preparer := autorest.CreatePreparer(
		autorest.AsGet(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{})
}

// ListSender sends the List request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) ListSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client, req)
}

// ListResponder handles the response to the List request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) ListResponder(resp *http.Response) (result InfraRoleInstanceList, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}

// ListNextResults retrieves the next set of results, if any.
func (client InfraRoleInstancesClient) ListNextResults(lastResults InfraRoleInstanceList) (result InfraRoleInstanceList, err error) {
	req, err := lastResults.InfraRoleInstanceListPreparer()
	if err != nil {
		return result, autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", nil, "Failure preparing next results request")
	}
	if req == nil {
		return
	}

	resp, err := client.ListSender(req)
	if err != nil {
		result.Response = autorest.Response{Response: resp}
		return result, autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", resp, "Failure sending next results request")
	}

	result, err = client.ListResponder(resp)
	if err != nil {
		err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "List", resp, "Failure responding to next results request")
	}

	return
}

// ListComplete gets all elements from the list without paging.
func (client InfraRoleInstancesClient) ListComplete(location string, filter string, cancel <-chan struct{}) (<-chan InfraRoleInstance, <-chan error) {
	resultChan := make(chan InfraRoleInstance)
	errChan := make(chan error, 1)
	go func() {
		defer func() {
			close(resultChan)
			close(errChan)
		}()
		list, err := client.List(location, filter)
		if err != nil {
			errChan <- err
			return
		}
		if list.Value != nil {
			for _, item := range *list.Value {
				select {
				case <-cancel:
					return
				case resultChan <- item:
					// Intentionally left blank
				}
			}
		}
		for list.NextLink != nil {
			list, err = client.ListNextResults(list)
			if err != nil {
				errChan <- err
				return
			}
			if list.Value != nil {
				for _, item := range *list.Value {
					select {
					case <-cancel:
						return
					case resultChan <- item:
						// Intentionally left blank
					}
				}
			}
		}
	}()
	return resultChan, errChan
}

// PowerOff power off an infra role instance. This method may poll for completion. Polling can be canceled by passing
// the cancel channel argument. The channel will be used to cancel polling and any outstanding HTTP requests.
//
// location is location of the resource. infraRoleInstance is name of an infra role instance.
func (client InfraRoleInstancesClient) PowerOff(location string, infraRoleInstance string, cancel <-chan struct{}) (<-chan OperationStatus, <-chan error) {
	resultChan := make(chan OperationStatus, 1)
	errChan := make(chan error, 1)
	if err := validation.Validate([]validation.Validation{}); err != nil {
		errChan <- validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "PowerOff")
		close(errChan)
		close(resultChan)
		return resultChan, errChan
	}

	go func() {
		var err error
		var result OperationStatus
		defer func() {
			if err != nil {
				errChan <- err
			}
			resultChan <- result
			close(resultChan)
			close(errChan)
		}()
		req, err := client.PowerOffPreparer(location, infraRoleInstance, cancel)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOff", nil, "Failure preparing request")
			return
		}

		resp, err := client.PowerOffSender(req)
		if err != nil {
			result.Response = autorest.Response{Response: resp}
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOff", resp, "Failure sending request")
			return
		}

		result, err = client.PowerOffResponder(resp)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOff", resp, "Failure responding to request")
		}
	}()
	return resultChan, errChan
}

// PowerOffPreparer prepares the PowerOff request.
func (client InfraRoleInstancesClient) PowerOffPreparer(location string, infraRoleInstance string, cancel <-chan struct{}) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"infraRoleInstance": autorest.Encode("path", infraRoleInstance),
		"location":          autorest.Encode("path", location),
		"subscriptionId":    autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}

	preparer := autorest.CreatePreparer(
		autorest.AsPost(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances/{infraRoleInstance}/PowerOff", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{Cancel: cancel})
}

// PowerOffSender sends the PowerOff request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) PowerOffSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client,
		req,
		azure.DoPollForAsynchronous(client.PollingDelay))
}

// PowerOffResponder handles the response to the PowerOff request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) PowerOffResponder(resp *http.Response) (result OperationStatus, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK, http.StatusAccepted, http.StatusInternalServerError),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}

// PowerOn power on an infra role instance. This method may poll for completion. Polling can be canceled by passing the
// cancel channel argument. The channel will be used to cancel polling and any outstanding HTTP requests.
//
// location is location of the resource. infraRoleInstance is name of an infra role instance.
func (client InfraRoleInstancesClient) PowerOn(location string, infraRoleInstance string, cancel <-chan struct{}) (<-chan OperationStatus, <-chan error) {
	resultChan := make(chan OperationStatus, 1)
	errChan := make(chan error, 1)
	if err := validation.Validate([]validation.Validation{}); err != nil {
		errChan <- validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "PowerOn")
		close(errChan)
		close(resultChan)
		return resultChan, errChan
	}

	go func() {
		var err error
		var result OperationStatus
		defer func() {
			if err != nil {
				errChan <- err
			}
			resultChan <- result
			close(resultChan)
			close(errChan)
		}()
		req, err := client.PowerOnPreparer(location, infraRoleInstance, cancel)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOn", nil, "Failure preparing request")
			return
		}

		resp, err := client.PowerOnSender(req)
		if err != nil {
			result.Response = autorest.Response{Response: resp}
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOn", resp, "Failure sending request")
			return
		}

		result, err = client.PowerOnResponder(resp)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "PowerOn", resp, "Failure responding to request")
		}
	}()
	return resultChan, errChan
}

// PowerOnPreparer prepares the PowerOn request.
func (client InfraRoleInstancesClient) PowerOnPreparer(location string, infraRoleInstance string, cancel <-chan struct{}) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"infraRoleInstance": autorest.Encode("path", infraRoleInstance),
		"location":          autorest.Encode("path", location),
		"subscriptionId":    autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}

	preparer := autorest.CreatePreparer(
		autorest.AsPost(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances/{infraRoleInstance}/PowerOn", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{Cancel: cancel})
}

// PowerOnSender sends the PowerOn request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) PowerOnSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client,
		req,
		azure.DoPollForAsynchronous(client.PollingDelay))
}

// PowerOnResponder handles the response to the PowerOn request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) PowerOnResponder(resp *http.Response) (result OperationStatus, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK, http.StatusAccepted, http.StatusInternalServerError),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}

// Reboot reboot an infra role instance. This method may poll for completion. Polling can be canceled by passing the
// cancel channel argument. The channel will be used to cancel polling and any outstanding HTTP requests.
//
// location is location of the resource. infraRoleInstance is name of an infra role instance.
func (client InfraRoleInstancesClient) Reboot(location string, infraRoleInstance string, cancel <-chan struct{}) (<-chan OperationStatus, <-chan error) {
	resultChan := make(chan OperationStatus, 1)
	errChan := make(chan error, 1)
	if err := validation.Validate([]validation.Validation{}); err != nil {
		errChan <- validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "Reboot")
		close(errChan)
		close(resultChan)
		return resultChan, errChan
	}

	go func() {
		var err error
		var result OperationStatus
		defer func() {
			if err != nil {
				errChan <- err
			}
			resultChan <- result
			close(resultChan)
			close(errChan)
		}()
		req, err := client.RebootPreparer(location, infraRoleInstance, cancel)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Reboot", nil, "Failure preparing request")
			return
		}

		resp, err := client.RebootSender(req)
		if err != nil {
			result.Response = autorest.Response{Response: resp}
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Reboot", resp, "Failure sending request")
			return
		}

		result, err = client.RebootResponder(resp)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Reboot", resp, "Failure responding to request")
		}
	}()
	return resultChan, errChan
}

// RebootPreparer prepares the Reboot request.
func (client InfraRoleInstancesClient) RebootPreparer(location string, infraRoleInstance string, cancel <-chan struct{}) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"infraRoleInstance": autorest.Encode("path", infraRoleInstance),
		"location":          autorest.Encode("path", location),
		"subscriptionId":    autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}

	preparer := autorest.CreatePreparer(
		autorest.AsPost(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances/{infraRoleInstance}/Reboot", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{Cancel: cancel})
}

// RebootSender sends the Reboot request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) RebootSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client,
		req,
		azure.DoPollForAsynchronous(client.PollingDelay))
}

// RebootResponder handles the response to the Reboot request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) RebootResponder(resp *http.Response) (result OperationStatus, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK, http.StatusAccepted, http.StatusInternalServerError),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}

// Shutdown shut down an infra role instance. This method may poll for completion. Polling can be canceled by passing
// the cancel channel argument. The channel will be used to cancel polling and any outstanding HTTP requests.
//
// location is location of the resource. infraRoleInstance is name of an infra role instance.
func (client InfraRoleInstancesClient) Shutdown(location string, infraRoleInstance string, cancel <-chan struct{}) (<-chan OperationStatus, <-chan error) {
	resultChan := make(chan OperationStatus, 1)
	errChan := make(chan error, 1)
	if err := validation.Validate([]validation.Validation{}); err != nil {
		errChan <- validation.NewErrorWithValidationError(err, "fabric.InfraRoleInstancesClient", "Shutdown")
		close(errChan)
		close(resultChan)
		return resultChan, errChan
	}

	go func() {
		var err error
		var result OperationStatus
		defer func() {
			if err != nil {
				errChan <- err
			}
			resultChan <- result
			close(resultChan)
			close(errChan)
		}()
		req, err := client.ShutdownPreparer(location, infraRoleInstance, cancel)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Shutdown", nil, "Failure preparing request")
			return
		}

		resp, err := client.ShutdownSender(req)
		if err != nil {
			result.Response = autorest.Response{Response: resp}
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Shutdown", resp, "Failure sending request")
			return
		}

		result, err = client.ShutdownResponder(resp)
		if err != nil {
			err = autorest.NewErrorWithError(err, "fabric.InfraRoleInstancesClient", "Shutdown", resp, "Failure responding to request")
		}
	}()
	return resultChan, errChan
}

// ShutdownPreparer prepares the Shutdown request.
func (client InfraRoleInstancesClient) ShutdownPreparer(location string, infraRoleInstance string, cancel <-chan struct{}) (*http.Request, error) {
	pathParameters := map[string]interface{}{
		"infraRoleInstance": autorest.Encode("path", infraRoleInstance),
		"location":          autorest.Encode("path", location),
		"subscriptionId":    autorest.Encode("path", client.SubscriptionID),
	}

	const APIVersion = "2016-05-01"
	queryParameters := map[string]interface{}{
		"api-version": APIVersion,
	}

	preparer := autorest.CreatePreparer(
		autorest.AsPost(),
		autorest.WithBaseURL(client.BaseURI),
		autorest.WithPathParameters("/subscriptions/{subscriptionId}/resourceGroups/System.{location}/providers/Microsoft.Fabric.Admin/fabricLocations/{location}/infraRoleInstances/{infraRoleInstance}/Shutdown", pathParameters),
		autorest.WithQueryParameters(queryParameters))
	return preparer.Prepare(&http.Request{Cancel: cancel})
}

// ShutdownSender sends the Shutdown request. The method will close the
// http.Response Body if it receives an error.
func (client InfraRoleInstancesClient) ShutdownSender(req *http.Request) (*http.Response, error) {
	return autorest.SendWithSender(client,
		req,
		azure.DoPollForAsynchronous(client.PollingDelay))
}

// ShutdownResponder handles the response to the Shutdown request. The method always
// closes the http.Response Body.
func (client InfraRoleInstancesClient) ShutdownResponder(resp *http.Response) (result OperationStatus, err error) {
	err = autorest.Respond(
		resp,
		client.ByInspecting(),
		azure.WithErrorUnlessStatusCode(http.StatusOK, http.StatusAccepted, http.StatusInternalServerError),
		autorest.ByUnmarshallingJSON(&result),
		autorest.ByClosing())
	result.Response = autorest.Response{Response: resp}
	return
}
