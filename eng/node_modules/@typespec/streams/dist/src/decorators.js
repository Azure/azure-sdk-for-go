import { useStateMap } from "@typespec/compiler/utils";
import { StreamStateKeys } from "./lib.js";
const [getStreamOf, setStreamOf] = useStateMap(StreamStateKeys.streamOf);
export const $streamOfDecorator = (context, target, type) => {
    setStreamOf(context.program, target, type);
};
export function isStream(program, target) {
    return getStreamOf(program, target) !== undefined;
}
export { getStreamOf };
//# sourceMappingURL=decorators.js.map