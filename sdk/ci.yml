trigger:
  paths:
    include:
    - sdk/

pr:
  paths:
    include:
    - sdk/
    
stages:
- template: ../eng/pipelines/templates/jobs/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: ''
