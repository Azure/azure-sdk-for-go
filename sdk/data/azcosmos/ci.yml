# NOTE: Please refer to https://aka.ms/azsdk/engsys/ci-yaml before editing this file.
trigger:
  branches:
    include:
      - main
      - hotfix/*
      - release/*
  paths:
    include:
    - sdk/data/azcosmos/

pr:
  branches:
    include:
      - main
      - feature/*
      - hotfix/*
      - release/*
  paths:
    include:
    - sdk/data/azcosmos/

extends:
  template: /eng/pipelines/templates/jobs/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: 'data/azcosmos'
    UsePipelineProxy: false
    AdditionalStages:
      - stage: Emulator
        displayName: 'Cosmos Emulator'
        variables:
        - template: /eng/pipelines/templates/variables/globals.yml@self
        - template: /eng/pipelines/templates/variables/image.yml@self
        jobs:
        - job: DownloadAndRunCosmosEmulator
          displayName: Download and run Cosmos Emulator

          pool:
            name: $(WINDOWSPOOL)
            image: $(WINDOWSVMIMAGE)
            os: windows

          steps:
          - template: /eng/common/pipelines/templates/steps/cosmos-emulator.yml@self
            parameters:
              StartParameters: '/noexplorer /noui /enablepreview /disableratelimiting /enableaadauthentication /partitioncount=50 /consistency=Strong /EnableSqlComputeEndpoint'
          - powershell: |
              $Key = 'C2y6yDjf5/R+ob0N8A7Cgv30VRDJIWEHLM+4QDU5DE2nQ9nDuVTqobD4b8mGGyPMbIZnqyMsEcaGQy67XIw/Jw=='
              $password = ConvertTo-SecureString -String $Key -Force -AsPlainText
              $cert = Get-ChildItem cert:\LocalMachine\My | Where-Object { $_.FriendlyName -eq "DocumentDbEmulatorCertificate" }
              Export-PfxCertificate -Cert $cert -FilePath ".\CosmosDbEmulatorCert.pfx" -Password $password | Out-Null
              $cert = New-Object System.Security.Cryptography.X509Certificates.X509Certificate2
              $cert.Import(".\CosmosDbEmulatorCert.pfx", $Key, "DefaultKeySet")
              $cert | Export-Certificate -FilePath "$env:temp\CosmosDbEmulatorCert.cer" -Type CERT
            displayName: 'Export Cosmos DB Emulator Certificate'
          - template: /eng/common/pipelines/templates/steps/verify-agent-os.yml@self
            parameters:
              AgentImage: windows

          - task: GoTool@0
            inputs:
              version: '1.26.1'
            displayName: "Select Go Version"

          - template: /eng/pipelines/templates/steps/create-go-workspace.yml@self

          - template:  /eng/pipelines/templates/steps/build-test.yml@self
            parameters:
              ServiceDirectory: 'data/azcosmos'
              Image: $(vm.image)
              GoVersion: '1.26.1'
              EnableRaceDetector: true
              EnvVars:
                EMULATOR: 'true'

      # The v2 driver binding needs a native library, a C toolchain and a build tag, none of which
      # the stage above provides, so it runs separately rather than complicating that one.
      #
      # It runs against the driver's own in-memory emulator rather than the containerized one. That
      # emulator is the same one the driver's Rust tests use, so the binding is exercised against
      # what the driver is developed against; it is a plain process, so the stage needs no
      # container runtime; it creates its databases and containers from its config, so nothing has
      # to be provisioned separately; and it reports its endpoints as JSON on stdout, so they are
      # read rather than assumed. It builds from the same checkout as the driver.
      - stage: DriverEmulator
        displayName: 'Cosmos Emulator (v2 driver)'
        variables:
        - template: /eng/pipelines/templates/variables/globals.yml@self
        - template: /eng/pipelines/templates/variables/image.yml@self
        # Pinned rather than floating: the C ABI is only guaranteed compatible within a version,
        # and this has to move in lockstep with nativeDriverVersion in driver.go.
        - name: DriverRepoCommit
          value: 2bc179cf6ff5c22e249fd10d944c8e946bb712d2
        - name: DriverRepoDir
          value: $(Agent.TempDirectory)/cosmos-driver
        - name: DriverLibDir
          value: $(Agent.TempDirectory)/cosmos-driver/target/release
        jobs:
        - job: BuildDriverAndRunEmulator
          displayName: Build the driver and run the emulator tests

          pool:
            name: $(LINUXPOOL)
            image: $(LINUXVMIMAGE)
            os: linux

          steps:
          - template: /eng/common/pipelines/templates/steps/verify-agent-os.yml@self
            parameters:
              AgentImage: ubuntu

          - script: |
              set -euo pipefail
              git clone --filter=blob:none --no-checkout \
                https://github.com/Azure/azure-sdk-for-rust.git "$(DriverRepoDir)"
              cd "$(DriverRepoDir)"
              git checkout $(DriverRepoCommit)
            displayName: 'Fetch the Cosmos driver source'

          - script: |
              set -euo pipefail
              cd "$(DriverRepoDir)"
              cargo build --release \
                -p azure_data_cosmos_driver_native \
                -p azure_data_cosmos_emulator
              # Fail loudly here rather than leaving the tests to fail at link time, or the
              # emulator step to fail on a missing binary, with a less obvious message.
              test -f "$(DriverLibDir)/libazurecosmosdriver.a"
              test -x "$(DriverLibDir)/azure_data_cosmos_emulator"
            displayName: 'Build the Cosmos driver and emulator'

          # The binding links the archive from a fixed path inside the module, because a cgo
          # directive cannot take a path from the environment. Staging it here is what stands in
          # for the per-target driver modules until they exist.
          - script: |
              set -euo pipefail
              mkdir -p sdk/data/azcosmos/internal/native/lib
              cp "$(DriverLibDir)/libazurecosmosdriver.a" \
                sdk/data/azcosmos/internal/native/lib/
            displayName: 'Stage the driver archive'

          # The header the binding compiles against is vendored, so it has to match the library it
          # links against. A mismatch is an ABI mismatch, which fails at runtime rather than at
          # build time and is worth catching here.
          - script: |
              set -euo pipefail
              vendored='sdk/data/azcosmos/internal/native/azurecosmosdriver.h'
              upstream="$(DriverRepoDir)/sdk/cosmos/azure_data_cosmos_driver_native/include/azurecosmosdriver.h"
              if ! diff -u "$vendored" "$upstream"; then
                echo "##[error] The vendored header does not match the driver at $(DriverRepoCommit)."
                echo "##[error] Re-vendor it and update nativeDriverVersion in driver.go."
                exit 1
              fi
            displayName: 'Verify the vendored header matches the driver'

          # The emulator writes one JSON record to stdout once every listener is bound, and logs to
          # stderr, so the endpoint is read from that record rather than guessed. Its config asks
          # for an OS-assigned port, so nothing collides with whatever else is on the agent.
          - script: |
              set -euo pipefail
              config='sdk/data/azcosmos/internal/testdata/emulator-config.json'
              nohup "$(DriverLibDir)/azure_data_cosmos_emulator" --config "$config" \
                > "$(Agent.TempDirectory)/emulator-ready.json" \
                2> "$(Agent.TempDirectory)/emulator.log" &

              for _ in $(seq 1 60); do
                if grep -q '"event":"ready"' "$(Agent.TempDirectory)/emulator-ready.json" 2>/dev/null; then
                  endpoint=$(python3 -c "import json,sys; print(json.load(open(sys.argv[1]))['accountEndpoint'])" \
                    "$(Agent.TempDirectory)/emulator-ready.json")
                  echo "emulator ready at $endpoint"
                  echo "##vso[task.setvariable variable=EmulatorEndpoint]$endpoint"
                  exit 0
                fi
                sleep 1
              done
              echo '##[error] The emulator did not become ready.'
              tail -50 "$(Agent.TempDirectory)/emulator.log" || true
              exit 1
            displayName: 'Start the in-memory Cosmos emulator'

          - task: GoTool@0
            inputs:
              version: '1.26.1'
            displayName: "Select Go Version"

          - template: /eng/pipelines/templates/steps/create-go-workspace.yml@self

          # The shared template forwards EnvVars only to its test step, so its own build and vet
          # steps would compile the stub and leave the binding unchecked. Nothing else in the
          # repository builds with this tag, so without this the cgo code is never vetted at all.
          # Vetting does not link, so it needs no library on the path.
          - script: |
              set -euo pipefail
              cd sdk/data/azcosmos
              go build -tags=azcosmos_driver ./...
              go vet -tags=azcosmos_driver ./...
            displayName: 'Build and vet the driver binding'
            env:
              CGO_ENABLED: '1'

          - template:  /eng/pipelines/templates/steps/build-test.yml@self
            parameters:
              ServiceDirectory: 'data/azcosmos'
              Image: $(vm.image)
              GoVersion: '1.26.1'
              EnableRaceDetector: true
              EnvVars:
                EMULATOR: 'true'
                AZCOSMOS_ENDPOINT: $(EmulatorEndpoint)
                # The binding is behind a build tag, and the shared test template has no parameter
                # for one, so it is injected through the go command's own environment variable
                # rather than by changing a template the Go central team owns.
                GOFLAGS: '-tags=azcosmos_driver'
                CGO_ENABLED: '1'
