// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License. See License.txt in the project root for license information.
// Code generated by Microsoft (R) Go Code Generator. DO NOT EDIT.

package armcontainerserviceaimanager

import "time"

// AIManager - The AI Manager resource. For more information, see https://aka.ms/aks/aimanager.
type AIManager struct {
	// REQUIRED; The geo-location where the resource lives
	Location *string

	// The managed service identities assigned to this resource.
	Identity *ManagedServiceIdentity

	// The resource-specific properties for this resource.
	Properties *AIManagerProperties

	// Resource tags.
	Tags map[string]*string

	// READ-ONLY; If eTag is provided in the response body, it may also be provided as a header per the normal etag convention.
	// Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in
	// the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header
	// fields.
	ETag *string

	// READ-ONLY; Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
	ID *string

	// READ-ONLY; The name of the resource
	Name *string

	// READ-ONLY; Azure Resource Manager metadata containing createdBy and modifiedBy information.
	SystemData *SystemData

	// READ-ONLY; The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
	Type *string
}

// AIManagerListResult - The response of a AIManager list operation.
type AIManagerListResult struct {
	// REQUIRED; The AIManager items on this page
	Value []*AIManager

	// The link to the next page of items
	NextLink *string
}

// AIManagerNamespace - The AI Manager namespace resource.
type AIManagerNamespace struct {
	// The resource-specific properties for this resource.
	Properties *AIManagerNamespaceProperties

	// READ-ONLY; If eTag is provided in the response body, it may also be provided as a header per the normal etag convention.
	// Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in
	// the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header
	// fields.
	ETag *string

	// READ-ONLY; Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
	ID *string

	// READ-ONLY; The name of the resource
	Name *string

	// READ-ONLY; Azure Resource Manager metadata containing createdBy and modifiedBy information.
	SystemData *SystemData

	// READ-ONLY; The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
	Type *string
}

// AIManagerNamespaceListResult - The response of a AIManagerNamespace list operation.
type AIManagerNamespaceListResult struct {
	// REQUIRED; The AIManagerNamespace items on this page
	Value []*AIManagerNamespace

	// The link to the next page of items
	NextLink *string
}

// AIManagerNamespaceProperties - AI Manager namespace properties.
type AIManagerNamespaceProperties struct {
	// Annotations applied to the Kubernetes namespace.
	Annotations map[string]*string

	// Labels applied to the Kubernetes namespace.
	Labels map[string]*string

	// READ-ONLY; The status of the last operation.
	ProvisioningState *AIManagerNamespaceProvisioningState
}

// AIManagerPatch - The AI Manager resource patch model.
type AIManagerPatch struct {
	// The managed service identities assigned to this resource.
	Identity *ManagedServiceIdentity

	// Resource tags.
	Tags map[string]*string
}

// AIManagerProperties - AI Manager properties.
type AIManagerProperties struct {
	// Delete options of the AI Manager. Defaults to `Delete` if not specified.
	DeletePolicy *DeletePolicy

	// READ-ONLY; The name of the managed resource group created by the AI Manager to hold underlying infrastructure resources.
	ManagedResourceGroupName *string

	// READ-ONLY; The status of the last operation.
	ProvisioningState *AIManagerProvisioningState
}

// AIModel - An AI model exposed by Microsoft.ContainerService. Read-only, globally-shared catalog entry that is platform-maintained
// and auto-provisioned by the resource provider. Can be referenced by `ModelDeployment` resources.
type AIModel struct {
	// The resource-specific properties for this resource.
	Properties *AIModelProperties

	// READ-ONLY; Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
	ID *string

	// READ-ONLY; The name of the resource
	Name *string

	// READ-ONLY; Azure Resource Manager metadata containing createdBy and modifiedBy information.
	SystemData *SystemData

	// READ-ONLY; The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
	Type *string
}

// AIModelListResult - The response of a AIModel list operation.
type AIModelListResult struct {
	// REQUIRED; The AIModel items on this page
	Value []*AIModel

	// The link to the next page of items
	NextLink *string
}

// AIModelProperties - AI model properties.
type AIModelProperties struct {
	// READ-ONLY; The Hugging Face model identifier in `<org>/<repo>` form, e.g. `microsoft/Phi-4-mini-instruct`. Uniquely identifies
	// the upstream model that backs this catalog entry.
	ModelID *string

	// READ-ONLY; Specification of the model.
	Spec *ModelSpec

	// READ-ONLY; An optional, free-form description of the model.
	Description *string
}

// AutoscaleProfile - Autoscaling configuration: scale replica count between a minimum and maximum.
type AutoscaleProfile struct {
	// REQUIRED; The minimum number of replicas. Must be at least `1`; scale-to-zero is not supported in autoscale mode (see `ScalingProfile`).
	MinReplicas *int32

	// The maximum number of replicas. If not specified, the service derives a default from the subscription GPU quota.
	MaxReplicas *int32
}

// CalculateCostPlan - A GPU SKU pricing plan returned by the `calculateCost` action. Describes the cost of running a single
// model replica on the specified `vmSize`. To estimate the cost of running multiple replicas, scale `totalHourlyPrice` by
// the desired replica count, bounded by `maxAvailableReplicas`.
type CalculateCostPlan struct {
	// READ-ONLY; Whether the caller can actually deploy this plan today (region availability, GPU quota, model fit, etc.). This
	// field gates the mutually exclusive properties on this model:
	//
	//   - When `feasible` is `true`: `totalHourlyPrice` is set and `infeasibilityReason` is omitted.
	//   - When `feasible` is `false`: `infeasibilityReason` is set and `totalHourlyPrice` is omitted.
	Feasible *bool

	// READ-ONLY; Maximum number of replicas the caller's subscription can deploy on this SKU today, computed from the available
	// GPU quota in the target region.
	MaxAvailableReplicas *int32

	// READ-ONLY; On-demand hourly price for a single VM of this SKU, in `currency`.
	VMHourlyPrice *float64

	// READ-ONLY; Azure VM SKU, e.g. "Standard_ND96isr_H100_v5". Matches the value accepted by `ModelDeploymentProperties.vmSize`.
	VMSize *string

	// READ-ONLY; Number of VMs required to host one replica on this SKU.
	VMsPerReplica *int32

	// READ-ONLY; Reason explaining why the plan is not deployable. This is a per-plan annotation, not an ARM error envelope.
	InfeasibilityReason *InfeasibilityReason

	// READ-ONLY; UTC timestamp of the price snapshot used for this plan.
	PriceAsOf *time.Time

	// READ-ONLY; Resolved quantization on this SKU.
	Quantization *string

	// READ-ONLY; Estimated relative inference performance of a single model replica on this SKU. Omitted when an estimate is
	// unavailable.
	ServingPerformanceEstimation *ServingPerformanceEstimation

	// READ-ONLY; Projected hourly cost for one replica (`vmsPerReplica` VMs), in `currency`.
	TotalHourlyPrice *float64
}

// CalculateCostRequest - Request body for the AI model `calculateCost` action.
type CalculateCostRequest struct {
}

// CalculateCostResponse - Response body for the AI model `calculateCost` action.
type CalculateCostResponse struct {
	// READ-ONLY; ISO 4217 currency code, e.g. "USD".
	Currency *string

	// READ-ONLY; Ranked list of GPU SKU pricing plans. Feasible plans first, ordered by `totalHourlyPrice` ascending; infeasible
	// plans last.
	Plans []*CalculateCostPlan
}

// CredentialResult - The credential result response.
type CredentialResult struct {
	// READ-ONLY; The name of the credential.
	Name *string

	// READ-ONLY; Base64-encoded Kubernetes configuration file.
	Value []byte
}

// CredentialResults - The list credential result response.
type CredentialResults struct {
	// READ-ONLY; Array of credential results.
	Kubeconfigs []*CredentialResult
}

// CredentialValue - A credential value. Exactly one variant must be set.
// In the current API version, only the `inline` variant is supported. Future
// API versions are expected to add additional credential kinds (for example,
// managed identity and Key Vault secret references) as sibling variants on
// this model.
type CredentialValue struct {
	// An inline credential containing a secret value supplied in the request payload.
	Inline *InlineCredential
}

// InfeasibilityReason - Reason explaining why a `CalculateCostPlan` is not deployable. This is a per-plan annotation surfaced
// inside a successful `calculateCost` response, not an ARM error envelope.
type InfeasibilityReason struct {
	// READ-ONLY; Machine-readable reason code.
	Code *InfeasibleCode

	// READ-ONLY; Human-readable message accompanying `code`.
	Message *string
}

// InlineCredential - A credential provided inline.
type InlineCredential struct {
	// REQUIRED; The access token, password, or other secret value.
	Value *string
}

// ManagedServiceIdentity - Managed service identity (system assigned and/or user assigned identities)
type ManagedServiceIdentity struct {
	// REQUIRED; The type of managed identity assigned to this resource.
	Type *ManagedServiceIdentityType

	// The identities assigned to this resource by the user.
	UserAssignedIdentities map[string]*UserAssignedIdentity

	// READ-ONLY; The service principal ID of the system assigned identity. This property will only be provided for a system assigned
	// identity.
	PrincipalID *string

	// READ-ONLY; The tenant ID of the system assigned identity. This property will only be provided for a system assigned identity.
	TenantID *string
}

// ManualScalingProfile - Manual scaling configuration: fixed replica count.
type ManualScalingProfile struct {
	// REQUIRED; Fixed number of replicas. May be `0` to stop serving traffic while keeping the deployment configuration (see
	// `ScalingProfile`).
	Replicas *int32
}

// ModelDeployment - A running deployment of a model in an AI Manager namespace.
// PUT (create or update) on this resource is a full replace: the request body
// represents the complete desired state, and any optional property omitted
// from the body is reset to its default value (or cleared, if it has no
// default). Callers must always send the full desired state on every PUT.
type ModelDeployment struct {
	// The resource-specific properties for this resource.
	Properties *ModelDeploymentProperties

	// READ-ONLY; If eTag is provided in the response body, it may also be provided as a header per the normal etag convention.
	// Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in
	// the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header
	// fields.
	ETag *string

	// READ-ONLY; Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
	ID *string

	// READ-ONLY; The name of the resource
	Name *string

	// READ-ONLY; Azure Resource Manager metadata containing createdBy and modifiedBy information.
	SystemData *SystemData

	// READ-ONLY; The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
	Type *string
}

// ModelDeploymentListResult - The response of a ModelDeployment list operation.
type ModelDeploymentListResult struct {
	// REQUIRED; The ModelDeployment items on this page
	Value []*ModelDeployment

	// The link to the next page of items
	NextLink *string
}

// ModelDeploymentOverrides - User overrides for a model deployment.
type ModelDeploymentOverrides struct {
	// Experimental free-form override key/value pairs. Subject to change without notice; not part of the stable contract. Recognized
	// keys are documented per release and may be added, renamed, or removed at any time.
	Values map[string]*string
}

// ModelDeploymentProperties - Model deployment properties.
type ModelDeploymentProperties struct {
	// REQUIRED; Full ARM resource id of the model to deploy. Phase 1 accepts an `AIModel` resource id only. Immutable after creation.
	ModelResourceID *string

	// REQUIRED; Azure VM SKU used to host the deployment, e.g. "Standard_NC96ads_A100_v4". Immutable after creation.
	VMSize *string

	// Full ARM resource id of a `ModelSource` to use when pulling artifacts for this deployment. Immutable after creation.
	ModelSourceResourceID *string

	// User overrides layered on top of profile resolution.
	Overrides *ModelDeploymentOverrides

	// Runtime performance mode.
	PerformanceMode *ModelDeploymentPerformanceMode

	// Scaling configuration for the deployment. Provide either `manual` (fixed replica count) or `autoscale` (autoscaling between
	// min/max replicas), but not both.
	Scale *ScalingProfile

	// READ-ONLY; The status of the last reconciliation.
	ProvisioningState *ModelDeploymentProvisioningState

	// READ-ONLY; Runtime status, populated once reconciliation begins.
	Status *ModelDeploymentStatus
}

// ModelDeploymentStatus - The runtime status of a model deployment. All fields are read-only and populated once reconciliation
// has started.
type ModelDeploymentStatus struct {
	// READ-ONLY; The current number of ready replicas serving traffic.
	CurrentReplicas *int32

	// READ-ONLY; The desired replica count reported by the controller. Equals `properties.scale.manual.replicas` when manual
	// scaling is used; current target replica count derived from autoscaler otherwise.
	DesiredReplicas *int32

	// READ-ONLY; The inference endpoint URL exposed by the deployment, once ready.
	Endpoint *string

	// READ-ONLY; The inference engine used to serve the model, e.g. "vllm".
	Engine *string

	// READ-ONLY; The version of the inference engine, e.g. "0.17".
	EngineVersion *string

	// READ-ONLY; Estimated total time, in seconds, for the deployment to become ready end-to-end (GPU vm provisioning, image/weight
	// pull, engine warm-up).
	EstimatedProvisionTimeSeconds *int32

	// READ-ONLY; The maximum model context length, in tokens, configured for this deployment.
	MaxModelLen *int32

	// READ-ONLY; The peak tokens per minute measured by live stress test.
	PeakTokensPerMinute *int32

	// READ-ONLY; The quantization level applied to the model weights, e.g. "fp16", "awq-int4".
	Quantization *string
}

// ModelSource - A model source registered with an AI Manager. Describes an external model registry (e.g. Hugging Face) and
// the credentials the platform uses to pull artifacts from it.
type ModelSource struct {
	// The resource-specific properties for this resource.
	Properties *ModelSourceProperties

	// READ-ONLY; If eTag is provided in the response body, it may also be provided as a header per the normal etag convention.
	// Entity tags are used for comparing two or more entities from the same requested resource. HTTP/1.1 uses entity tags in
	// the etag (section 14.19), If-Match (section 14.24), If-None-Match (section 14.26), and If-Range (section 14.27) header
	// fields.
	ETag *string

	// READ-ONLY; Fully qualified resource ID for the resource. Ex - /subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/{resourceProviderNamespace}/{resourceType}/{resourceName}
	ID *string

	// READ-ONLY; The name of the resource
	Name *string

	// READ-ONLY; Azure Resource Manager metadata containing createdBy and modifiedBy information.
	SystemData *SystemData

	// READ-ONLY; The type of the resource. E.g. "Microsoft.Compute/virtualMachines" or "Microsoft.Storage/storageAccounts"
	Type *string
}

// ModelSourceListResult - The response of a ModelSource list operation.
type ModelSourceListResult struct {
	// REQUIRED; The ModelSource items on this page
	Value []*ModelSource

	// The link to the next page of items
	NextLink *string
}

// ModelSourceProperties - Model source properties.
type ModelSourceProperties struct {
	// REQUIRED; Model source type. Constrains the legal authentication kinds. Immutable after creation.
	SourceType *ModelSourceType

	// Credential the platform uses to authenticate to the source. Optional for public sources (e.g. ungated Hugging Face models).
	Credential *CredentialValue

	// An optional, free-form description of the source.
	Description *string

	// READ-ONLY; The status of the last operation.
	ProvisioningState *ResourceProvisioningState
}

// ModelSpec - The specification of a model. All fields are read-only.
type ModelSpec struct {
	// READ-ONLY; Whether access to the model is restricted and requires credential.
	IsRestricted *bool

	// READ-ONLY; The maximum context length supported by the model, in tokens.
	MaxContextLength *int32

	// READ-ONLY; The license of the model, when known. SPDX license identifier, e.g. `mit`, `apache-2.0`.
	License *string
}

// NamespaceAccessInfo - Access information for an AI Manager namespace, including the OpenAI-compatible gateway endpoint
// and the API keys used to authenticate against it.
type NamespaceAccessInfo struct {
	// READ-ONLY; OpenAI-compatible inference gateway base URL (for example, `https://team-alpha.<cluster>.<region>.aksapp.io/v1`).
	Endpoint *string

	// READ-ONLY; Primary API key. Send as `Authorization: Bearer <key>` or `api-key: <key>`. Treat as secret; do not log or persist
	// in plaintext.
	PrimaryKey *string

	// READ-ONLY; Secondary API key, accepted by the gateway in the same headers as `primaryKey`. Generated independently when
	// the namespace is created, then overwritten by the previous `primaryKey` on each `rotateKeys` call so clients can roll over
	// without downtime. Treat as secret; do not log or persist in plaintext.
	SecondaryKey *string

	// READ-ONLY; UTC time the keys were last rotated by `rotateKeys`. Absent until the first rotation. Clients can use this to
	// detect rotation and refresh cached credentials.
	LastRotatedAt *time.Time
}

// Operation - REST API Operation
//
// Details of a REST API operation, returned from the Resource Provider Operations API
type Operation struct {
	// Localized display information for this particular operation.
	Display *OperationDisplay

	// READ-ONLY; Extensible enum. Indicates the action type. "Internal" refers to actions that are for internal only APIs.
	ActionType *ActionType

	// READ-ONLY; Whether the operation applies to data-plane. This is "true" for data-plane operations and "false" for Azure
	// Resource Manager/control-plane operations.
	IsDataAction *bool

	// READ-ONLY; The name of the operation, as per Resource-Based Access Control (RBAC). Examples: "Microsoft.Compute/virtualMachines/write",
	// "Microsoft.Compute/virtualMachines/capture/action"
	Name *string

	// READ-ONLY; The intended executor of the operation; as in Resource Based Access Control (RBAC) and audit logs UX. Default
	// value is "user,system"
	Origin *Origin
}

// OperationDisplay - Localized display information for an operation.
type OperationDisplay struct {
	// READ-ONLY; The short, localized friendly description of the operation; suitable for tool tips and detailed views.
	Description *string

	// READ-ONLY; The concise, localized friendly name for the operation; suitable for dropdowns. E.g. "Create or Update Virtual
	// Machine", "Restart Virtual Machine".
	Operation *string

	// READ-ONLY; The localized friendly form of the resource provider name, e.g. "Microsoft Monitoring Insights" or "Microsoft
	// Compute".
	Provider *string

	// READ-ONLY; The localized friendly name of the resource type related to this operation. E.g. "Virtual Machines" or "Job
	// Schedule Collections".
	Resource *string
}

// OperationListResult - A list of REST API operations supported by an Azure Resource Provider. It contains an URL link to
// get the next set of results.
type OperationListResult struct {
	// REQUIRED; The Operation items on this page
	Value []*Operation

	// The link to the next page of items
	NextLink *string
}

// ScalingProfile - Scaling configuration for a model deployment. Exactly one of `manual` or
// `autoscale` must be set.
// This mutual-exclusion constraint is enforced by the service at request
// validation time, not by the schema. A PUT request that sets both `manual`
// and `autoscale`, or sets neither, is rejected with HTTP 400 (Bad Request)
// and an `InvalidScalingProfile` error code;
// Scale-to-zero semantics differ between the two modes:
//
//   - `manual` permits `replicas: 0`. This is an explicit operator action to
//
// stop serving traffic while keeping the `ModelDeployment` resource (and
// its configuration) in place. While at zero replicas the endpoint
// returns errors for inference requests, and the deployment releases its
// GPU capacity.
//
//   - `autoscale` does not permit `minReplicas: 0`. Autoscaling decisions are
//
// driven by serving-server runtime metrics (request rate, queue depth,
// GPU utilization); at zero replicas there is no signal for the
// autoscaler to scale back up from. Combined with GPU cold-start time
// (on the order of minutes) and constrained regional GPU capacity, a
// scale-from-zero event would produce unacceptable first-request latency
// and a high risk of capacity unavailability. Callers that want
// autoscaling with an idle state should delete the `ModelDeployment`
// instead.
type ScalingProfile struct {
	// Autoscaling configuration. Mutually exclusive with `manual`.
	Autoscale *AutoscaleProfile

	// Manual scaling configuration with a fixed replica count. Mutually exclusive with `autoscale`.
	Manual *ManualScalingProfile
}

// ServingPerformanceEstimation - Estimated relative inference performance of a single model replica on a given GPU SKU. Each
// metric is a scaling coefficient in the range `[0, 1]` relative to the best-performing SKU for this model, which scores
// `1`.
type ServingPerformanceEstimation struct {
	// READ-ONLY; Relative inference latency score in `[0, 1]`. Higher is better (`1` matches the best-performing SKU's latency
	// for this model). Note: this is a normalized score, not a raw latency ratio -- a larger value indicates lower latency.
	RelativeLatencyScore *float32

	// READ-ONLY; Relative inference throughput score in `[0, 1]`. Higher is better (`1` matches the best-performing SKU's throughput
	// for this model).
	RelativeThroughputScore *float32
}

// SystemData - Metadata pertaining to creation and last modification of the resource.
type SystemData struct {
	// The timestamp of resource creation (UTC).
	CreatedAt *time.Time

	// The identity that created the resource.
	CreatedBy *string

	// The type of identity that created the resource.
	CreatedByType *CreatedByType

	// The timestamp of resource last modification (UTC)
	LastModifiedAt *time.Time

	// The identity that last modified the resource.
	LastModifiedBy *string

	// The type of identity that last modified the resource.
	LastModifiedByType *CreatedByType
}

// UserAssignedIdentity - User assigned identity properties
type UserAssignedIdentity struct {
	// READ-ONLY; The client ID of the assigned identity.
	ClientID *string

	// READ-ONLY; The principal ID of the assigned identity.
	PrincipalID *string
}
